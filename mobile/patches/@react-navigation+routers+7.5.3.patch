diff --git a/node_modules/@react-navigation/routers/lib/module/BaseRouter.js b/node_modules/@react-navigation/routers/lib/module/BaseRouter.js
index e5f5b08..92af2ca 100644
--- a/node_modules/@react-navigation/routers/lib/module/BaseRouter.js
+++ b/node_modules/@react-navigation/routers/lib/module/BaseRouter.js
@@ -40,7 +40,7 @@ export const BaseRouter = {
               ...nextState,
               routes: nextState.routes.map(route => route.key ? route : {
                 ...route,
-                key: `${route.name}-${nanoid()}`
+                key: route.name + '-' + nanoid()
               })
             };
           }
diff --git a/node_modules/@react-navigation/routers/lib/module/CommonActions.js b/node_modules/@react-navigation/routers/lib/module/CommonActions.js
index b3f7cbb..b02cc04 100644
--- a/node_modules/@react-navigation/routers/lib/module/CommonActions.js
+++ b/node_modules/@react-navigation/routers/lib/module/CommonActions.js
@@ -9,15 +9,17 @@ export function navigate(...args) {
   if (typeof args[0] === 'string') {
     const [name, params, options] = args;
     if (typeof options === 'boolean') {
-      console.warn(`Passing a boolean as the third argument to 'navigate' is deprecated. Pass '{ merge: true }' instead.`);
+      console.warn("Passing a boolean as the third argument to 'navigate' is deprecated. Pass '{ merge: true }' instead.");
     }
+    const merge = typeof options === 'boolean' ? options : options && 'merge' in options ? options.merge : undefined;
+    const pop = options && 'pop' in options ? options.pop : undefined;
     return {
       type: 'NAVIGATE',
       payload: {
         name,
         params,
-        merge: typeof options === 'boolean' ? options : options?.merge,
-        pop: options?.pop
+        merge,
+        pop
       }
     };
   } else {
diff --git a/node_modules/@react-navigation/routers/lib/module/DrawerRouter.js b/node_modules/@react-navigation/routers/lib/module/DrawerRouter.js
index 967abd4..aaa906d 100644
--- a/node_modules/@react-navigation/routers/lib/module/DrawerRouter.js
+++ b/node_modules/@react-navigation/routers/lib/module/DrawerRouter.js
@@ -77,7 +77,7 @@ export function DrawerRouter({
         default: defaultStatus,
         stale: false,
         type: 'drawer',
-        key: `drawer-${nanoid()}`
+        key: 'drawer-' + nanoid()
       };
     },
     getRehydratedState(partialState, {
@@ -102,7 +102,7 @@ export function DrawerRouter({
         ...state,
         default: defaultStatus,
         type: 'drawer',
-        key: `drawer-${nanoid()}`
+        key: 'drawer-' + nanoid()
       };
     },
     getStateForRouteFocus(state, key) {
diff --git a/node_modules/@react-navigation/routers/lib/module/StackRouter.js b/node_modules/@react-navigation/routers/lib/module/StackRouter.js
index c1459d5..33a789a 100644
--- a/node_modules/@react-navigation/routers/lib/module/StackRouter.js
+++ b/node_modules/@react-navigation/routers/lib/module/StackRouter.js
@@ -38,14 +38,15 @@ export const StackActions = {
   },
   popTo(name, params, options) {
     if (typeof options === 'boolean') {
-      console.warn(`Passing a boolean as the third argument to 'popTo' is deprecated. Pass '{ merge: true }' instead.`);
+      console.warn("Passing a boolean as the third argument to 'popTo' is deprecated. Pass '{ merge: true }' instead.");
     }
+    const merge = typeof options === 'boolean' ? options : options && 'merge' in options ? options.merge : undefined;
     return {
       type: 'POP_TO',
       payload: {
         name,
         params,
-        merge: typeof options === 'boolean' ? options : options?.merge
+        merge
       }
     };
   }
@@ -62,12 +63,12 @@ export function StackRouter(options) {
       return {
         stale: false,
         type: 'stack',
-        key: `stack-${nanoid()}`,
+        key: 'stack-' + nanoid(),
         index: 0,
         routeNames,
         preloadedRoutes: [],
         routes: [{
-          key: `${initialRouteName}-${nanoid()}`,
+          key: initialRouteName + '-' + nanoid(),
           name: initialRouteName,
           params: routeParamList[initialRouteName]
         }]
@@ -83,24 +84,24 @@ export function StackRouter(options) {
       }
       const routes = state.routes.filter(route => routeNames.includes(route.name)).map(route => ({
         ...route,
-        key: route.key || `${route.name}-${nanoid()}`,
+        key: route.key || (route.name + '-' + nanoid()),
         params: routeParamList[route.name] !== undefined ? {
           ...routeParamList[route.name],
           ...route.params
         } : route.params
       }));
-      const preloadedRoutes = state.preloadedRoutes?.filter(route => routeNames.includes(route.name)).map(route => ({
+      const preloadedRoutes = state.preloadedRoutes ? state.preloadedRoutes.filter(route => routeNames.includes(route.name)).map(route => ({
         ...route,
-        key: route.key || `${route.name}-${nanoid()}`,
+        key: route.key || (route.name + '-' + nanoid()),
         params: routeParamList[route.name] !== undefined ? {
           ...routeParamList[route.name],
           ...route.params
         } : route.params
-      })) ?? [];
+      })) : [];
       if (routes.length === 0) {
         const initialRouteName = options.initialRouteName !== undefined ? options.initialRouteName : routeNames[0];
         routes.push({
-          key: `${initialRouteName}-${nanoid()}`,
+          key: initialRouteName + '-' + nanoid(),
           name: initialRouteName,
           params: routeParamList[initialRouteName]
         });
@@ -108,7 +109,7 @@ export function StackRouter(options) {
       return {
         stale: false,
         type: 'stack',
-        key: `stack-${nanoid()}`,
+        key: 'stack-' + nanoid(),
         index: routes.length - 1,
         routeNames,
         routes,
@@ -124,7 +125,7 @@ export function StackRouter(options) {
       if (routes.length === 0) {
         const initialRouteName = options.initialRouteName !== undefined && routeNames.includes(options.initialRouteName) ? options.initialRouteName : routeNames[0];
         routes.push({
-          key: `${initialRouteName}-${nanoid()}`,
+          key: initialRouteName + '-' + nanoid(),
           name: initialRouteName,
           params: routeParamList[initialRouteName]
         });
@@ -162,14 +163,14 @@ export function StackRouter(options) {
               return null;
             }
             const getId = options.routeGetIdList[action.payload.name];
-            const id = getId?.({
+            const id = getId ? getId({
               params: action.payload.params
-            });
+            }) : undefined;
 
             // Re-use preloaded route if available
-            let route = state.preloadedRoutes.find(route => route.name === action.payload.name && id === getId?.({
+            let route = state.preloadedRoutes.find(route => route.name === action.payload.name && id === (getId ? getId({
               params: route.params
-            }));
+            }) : undefined));
             if (!route) {
               route = createRouteFromAction({
                 action,
@@ -189,14 +190,14 @@ export function StackRouter(options) {
               return null;
             }
             const getId = options.routeGetIdList[action.payload.name];
-            const id = getId?.({
+            const id = getId ? getId({
               params: action.payload.params
-            });
+            }) : undefined;
             let route;
             if (id !== undefined) {
-              route = state.routes.findLast(route => route.name === action.payload.name && id === getId?.({
+              route = state.routes.findLast(route => route.name === action.payload.name && id === (getId ? getId({
                 params: route.params
-              }));
+              }) : undefined));
             } else if (action.type === 'NAVIGATE') {
               const currentRoute = state.routes[state.index];
 
@@ -208,9 +209,9 @@ export function StackRouter(options) {
               }
             }
             if (!route) {
-              route = state.preloadedRoutes.find(route => route.name === action.payload.name && id === getId?.({
+              route = state.preloadedRoutes.find(route => route.name === action.payload.name && id === (getId ? getId({
                 params: route.params
-              }));
+              }) : undefined));
             }
             let params;
             if (action.type === 'NAVIGATE' && action.payload.merge && route) {
@@ -252,7 +253,7 @@ export function StackRouter(options) {
               }
             } else {
               routes = [...state.routes, {
-                key: `${action.payload.name}-${nanoid()}`,
+                key: action.payload.name + '-' + nanoid(),
                 name: action.payload.name,
                 path: action.type === 'NAVIGATE' ? action.payload.path : undefined,
                 params
@@ -270,22 +271,22 @@ export function StackRouter(options) {
             if (!state.routeNames.includes(action.payload.name)) {
               return null;
             }
-            if (state.preloadedRoutes.find(route => route.name === action.payload.name && id === getId?.({
+            const getId = options.routeGetIdList[action.payload.name];
+            const id = getId ? getId({
+              params: action.payload.params
+            }) : undefined;
+            if (state.preloadedRoutes.find(route => route.name === action.payload.name && id === (getId ? getId({
               params: route.params
-            }))) {
+            }) : undefined))) {
               return null;
             }
 
             // If the route already exists, navigate to that
             let index = -1;
-            const getId = options.routeGetIdList[action.payload.name];
-            const id = getId?.({
-              params: action.payload.params
-            });
             if (id !== undefined) {
-              index = state.routes.findIndex(route => route.name === action.payload.name && id === getId?.({
+              index = state.routes.findIndex(route => route.name === action.payload.name && id === (getId ? getId({
                 params: route.params
-              }));
+              }) : undefined));
             } else if (state.routes[state.index].name === action.payload.name) {
               index = state.index;
             } else {
@@ -359,13 +360,13 @@ export function StackRouter(options) {
             // If the route already exists, navigate to it
             let index = -1;
             const getId = options.routeGetIdList[action.payload.name];
-            const id = getId?.({
+            const id = getId ? getId({
               params: action.payload.params
-            });
+            }) : undefined;
             if (id !== undefined) {
-              index = state.routes.findIndex(route => route.name === action.payload.name && id === getId?.({
+              index = state.routes.findIndex(route => route.name === action.payload.name && id === (getId ? getId({
                 params: route.params
-              }));
+              }) : undefined));
             } else if (state.routes[currentIndex].name === action.payload.name) {
               index = currentIndex;
             } else {
@@ -380,9 +381,9 @@ export function StackRouter(options) {
             // If the route doesn't exist, remove the current route and add the new one
             if (index === -1) {
               // Re-use preloaded route if available
-              let route = state.preloadedRoutes.find(route => route.name === action.payload.name && id === getId?.({
+              let route = state.preloadedRoutes.find(route => route.name === action.payload.name && id === (getId ? getId({
                 params: route.params
-              }));
+              }) : undefined));
               if (!route) {
                 route = createRouteFromAction({
                   action,
@@ -435,20 +436,20 @@ export function StackRouter(options) {
         case 'PRELOAD':
           {
             const getId = options.routeGetIdList[action.payload.name];
-            const id = getId?.({
+            const id = getId ? getId({
               params: action.payload.params
-            });
+            }) : undefined;
             let route;
             if (id !== undefined) {
-              route = state.routes.find(route => route.name === action.payload.name && id === getId?.({
+              route = state.routes.find(route => route.name === action.payload.name && id === (getId ? getId({
                 params: route.params
-              }));
+              }) : undefined));
             }
             if (route) {
               return {
                 ...state,
                 routes: state.routes.map(r => {
-                  if (r.key !== route?.key) {
+                  if (r.key !== (route ? route.key : undefined)) {
                     return r;
                   }
                   return {
@@ -463,9 +464,9 @@ export function StackRouter(options) {
             } else {
               return {
                 ...state,
-                preloadedRoutes: state.preloadedRoutes.filter(r => r.name !== action.payload.name || id !== getId?.({
+                preloadedRoutes: state.preloadedRoutes.filter(r => r.name !== action.payload.name || id !== (getId ? getId({
                   params: r.params
-                })).concat(createRouteFromAction({
+                }) : undefined)).concat(createRouteFromAction({
                   action,
                   routeParamList
                 }))
diff --git a/node_modules/@react-navigation/routers/lib/module/TabRouter.js b/node_modules/@react-navigation/routers/lib/module/TabRouter.js
index f3b26a9..f6981e2 100644
--- a/node_modules/@react-navigation/routers/lib/module/TabRouter.js
+++ b/node_modules/@react-navigation/routers/lib/module/TabRouter.js
@@ -64,7 +64,8 @@ const changeIndex = (state, index, backBehavior, initialRouteName) => {
       history = history.filter(it => it.type === 'route' ? it.key !== currentRoute.key : false);
     } else if (backBehavior === 'fullHistory') {
       const lastHistoryRouteItemIndex = history.findLastIndex(item => item.type === 'route');
-      if (currentRoute.key === history[lastHistoryRouteItemIndex]?.key) {
+      const lastHistoryRouteItem = lastHistoryRouteItemIndex >= 0 ? history[lastHistoryRouteItemIndex] : undefined;
+      if (currentRoute.key === (lastHistoryRouteItem ? lastHistoryRouteItem.key : undefined)) {
         // For full-history, only remove if it matches the last route
         // Useful for drawer, if current route was in history, then drawer state changed
         // Then we only need to move the route to the front
@@ -99,14 +100,14 @@ export function TabRouter({
       const index = initialRouteName !== undefined && routeNames.includes(initialRouteName) ? routeNames.indexOf(initialRouteName) : 0;
       const routes = routeNames.map(name => ({
         name,
-        key: `${name}-${nanoid()}`,
+        key: name + '-' + nanoid(),
         params: routeParamList[name]
       }));
       const history = getRouteHistory(routes, index, backBehavior, initialRouteName);
       return {
         stale: false,
         type: 'tab',
-        key: `tab-${nanoid()}`,
+        key: 'tab-' + nanoid(),
         index,
         routeNames,
         history,
@@ -127,25 +128,29 @@ export function TabRouter({
         return {
           ...route,
           name,
-          key: route && route.name === name && route.key ? route.key : `${name}-${nanoid()}`,
+          key: route && route.name === name && route.key ? route.key : name + '-' + nanoid(),
           params: routeParamList[name] !== undefined ? {
             ...routeParamList[name],
             ...(route ? route.params : undefined)
           } : route ? route.params : undefined
         };
       });
-      const index = Math.min(Math.max(routeNames.indexOf(state.routes[state?.index ?? 0]?.name), 0), routes.length - 1);
+      const stateIndex = state && state.index != null ? state.index : 0;
+      const focusedRoute = state.routes[stateIndex];
+      const focusedRouteName = focusedRoute ? focusedRoute.name : undefined;
+      const index = Math.min(Math.max(routeNames.indexOf(focusedRouteName), 0), routes.length - 1);
       const routeKeys = routes.map(route => route.key);
-      const history = state.history?.filter(it => routeKeys.includes(it.key)) ?? [];
+      const history = state.history ? state.history.filter(it => routeKeys.includes(it.key)) : [];
+      const preloadedRouteKeys = state.preloadedRouteKeys ? state.preloadedRouteKeys.filter(key => routeKeys.includes(key)) : [];
       return changeIndex({
         stale: false,
         type: 'tab',
-        key: `tab-${nanoid()}`,
+        key: 'tab-' + nanoid(),
         index,
         routeNames,
         history,
         routes,
-        preloadedRouteKeys: state.preloadedRouteKeys?.filter(key => routeKeys.includes(key)) ?? []
+        preloadedRouteKeys
       }, index, backBehavior, initialRouteName);
     },
     getStateForRouteNamesChange(state, {
@@ -155,7 +160,7 @@ export function TabRouter({
     }) {
       const routes = routeNames.map(name => state.routes.find(r => r.name === name && !routeKeyChanges.includes(r.name)) || {
         name,
-        key: `${name}-${nanoid()}`,
+        key: name + '-' + nanoid(),
         params: routeParamList[name]
       });
       const index = Math.max(0, routeNames.indexOf(state.routes[state.index].name));
@@ -200,13 +205,13 @@ export function TabRouter({
                   return route;
                 }
                 const getId = routeGetIdList[route.name];
-                const currentId = getId?.({
+                const currentId = getId ? getId({
                   params: route.params
-                });
-                const nextId = getId?.({
+                }) : undefined;
+                const nextId = getId ? getId({
                   params: action.payload.params
-                });
-                const key = currentId === nextId ? route.key : `${route.name}-${nanoid()}`;
+                }) : undefined;
+                const key = currentId === nextId ? route.key : route.name + '-' + nanoid();
                 let params;
                 if ((action.type === 'NAVIGATE' || action.type === 'NAVIGATE_DEPRECATED') && action.payload.merge && currentId === nextId) {
                   params = action.payload.params !== undefined || routeParamList[route.name] !== undefined ? {
@@ -265,7 +270,7 @@ export function TabRouter({
               return null;
             }
             const previousHistoryItem = state.history[state.history.length - 2];
-            const previousKey = previousHistoryItem?.key;
+            const previousKey = previousHistoryItem ? previousHistoryItem.key : undefined;
             const index = state.routes.findLastIndex(route => route.key === previousKey);
             if (index === -1) {
               return null;
@@ -294,13 +299,13 @@ export function TabRouter({
             }
             const route = state.routes[routeIndex];
             const getId = routeGetIdList[route.name];
-            const currentId = getId?.({
+            const currentId = getId ? getId({
               params: route.params
-            });
-            const nextId = getId?.({
+            }) : undefined;
+            const nextId = getId ? getId({
               params: action.payload.params
-            });
-            const key = currentId === nextId ? route.key : `${route.name}-${nanoid()}`;
+            }) : undefined;
+            const key = currentId === nextId ? route.key : route.name + '-' + nanoid();
             const params = createParamsFromAction({
               action,
               routeParamList
diff --git a/node_modules/@react-navigation/routers/lib/module/createRouteFromAction.js b/node_modules/@react-navigation/routers/lib/module/createRouteFromAction.js
index 5918a11..1cdccbd 100644
--- a/node_modules/@react-navigation/routers/lib/module/createRouteFromAction.js
+++ b/node_modules/@react-navigation/routers/lib/module/createRouteFromAction.js
@@ -10,7 +10,7 @@ export function createRouteFromAction({
     name
   } = action.payload;
   return {
-    key: `${name}-${nanoid()}`,
+    key: name + '-' + nanoid(),
     name,
     params: createParamsFromAction({
       action,
