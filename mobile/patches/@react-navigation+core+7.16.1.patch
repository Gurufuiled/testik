diff --git a/node_modules/@react-navigation/core/lib/module/BaseNavigationContainer.js b/node_modules/@react-navigation/core/lib/module/BaseNavigationContainer.js
index ee57b55..3ef12f2 100644
--- a/node_modules/@react-navigation/core/lib/module/BaseNavigationContainer.js
+++ b/node_modules/@react-navigation/core/lib/module/BaseNavigationContainer.js
@@ -125,7 +125,9 @@ export const BaseNavigationContainer = /*#__PURE__*/React.forwardRef(function Ba
     }
   });
   const resetRoot = useLatestCallback(state => {
-    const target = state?.key ?? keyedListeners.getState.root?.().key;
+    const rootGetter = keyedListeners.getState.root;
+    const rootState = rootGetter ? rootGetter() : undefined;
+    const target = state && state.key != null ? state.key : rootState ? rootState.key : undefined;
     if (target == null) {
       console.error(NOT_INITIALIZED_ERROR);
     } else {
@@ -136,7 +138,8 @@ export const BaseNavigationContainer = /*#__PURE__*/React.forwardRef(function Ba
     }
   });
   const getRootState = useLatestCallback(() => {
-    return keyedListeners.getState.root?.();
+    const rootGetter = keyedListeners.getState.root;
+    return rootGetter ? rootGetter() : undefined;
   });
   const getCurrentRoute = useLatestCallback(() => {
     const state = getRootState();
@@ -230,7 +233,9 @@ export const BaseNavigationContainer = /*#__PURE__*/React.forwardRef(function Ba
   React.useEffect(() => {
     if (!onReadyCalledRef.current && isReady()) {
       onReadyCalledRef.current = true;
-      onReadyRef.current?.();
+      if (onReadyRef.current) {
+        onReadyRef.current();
+      }
       emitter.emit({
         type: 'ready'
       });
@@ -260,21 +265,21 @@ export const BaseNavigationContainer = /*#__PURE__*/React.forwardRef(function Ba
                 path += ' > ';
               }
             } else if (!params && typeof curr === 'number' && prev === 'routes') {
-              path += pointer?.name;
+              path += pointer ? pointer.name : undefined;
             } else if (!params) {
-              path += ` > ${curr}`;
+              path += " > " + curr;
               params = true;
             } else {
               if (typeof curr === 'number' || /^[0-9]+$/.test(curr)) {
-                path += `[${curr}]`;
+                path += "[" + curr + "]";
               } else if (/^[a-z$_]+$/i.test(curr)) {
-                path += `.${curr}`;
+                path += "." + curr;
               } else {
-                path += `[${JSON.stringify(curr)}]`;
+                path += "[" + JSON.stringify(curr) + "]";
               }
             }
           }
-          const message = `Non-serializable values were found in the navigation state. Check:\n\n${path} (${reason})\n\nThis can break usage such as persisting and restoring state. This might happen if you passed non-serializable values such as function, class instances etc. in params. If you need to use components with callbacks in your options, you can use 'navigation.setOptions' instead. See https://reactnavigation.org/docs/troubleshooting#i-get-the-warning-non-serializable-values-were-found-in-the-navigation-state for more details.`;
+          const message = "Non-serializable values were found in the navigation state. Check:\n\n" + path + " (" + reason + ")\n\nThis can break usage such as persisting and restoring state. This might happen if you passed non-serializable values such as function, class instances etc. in params. If you need to use components with callbacks in your options, you can use 'navigation.setOptions' instead. See https://reactnavigation.org/docs/troubleshooting#i-get-the-warning-non-serializable-values-were-found-in-the-navigation-state for more details.";
           if (!serializableWarnings.includes(message)) {
             serializableWarnings.push(message);
             console.warn(message);
@@ -282,7 +287,8 @@ export const BaseNavigationContainer = /*#__PURE__*/React.forwardRef(function Ba
         }
         const duplicateRouteNamesResult = checkDuplicateRouteNames(hydratedState);
         if (duplicateRouteNamesResult.length) {
-          const message = `Found screens with the same name nested inside one another. Check:\n${duplicateRouteNamesResult.map(locations => `\n${locations.join(', ')}`)}\n\nThis can cause confusing behavior during navigation. Consider using unique names for each screen instead.`;
+          const duplicateMessageLines = duplicateRouteNamesResult.map(locations => "\n" + locations.join(', ')).join('');
+          const message = "Found screens with the same name nested inside one another. Check:\n" + duplicateMessageLines + "\n\nThis can cause confusing behavior during navigation. Consider using unique names for each screen instead.";
           if (!duplicateNameWarnings.includes(message)) {
             duplicateNameWarnings.push(message);
             console.warn(message);
@@ -306,7 +312,7 @@ export const BaseNavigationContainer = /*#__PURE__*/React.forwardRef(function Ba
       return;
     }
     const payload = action.payload;
-    let message = `The action '${action.type}'${payload ? ` with payload ${JSON.stringify(action.payload)}` : ''} was not handled by any navigator.`;
+    let message = "The action '" + action.type + "'" + (payload ? " with payload " + JSON.stringify(action.payload) : '') + " was not handled by any navigator.";
     switch (action.type) {
       case 'PRELOAD':
       case 'NAVIGATE':
@@ -314,24 +320,24 @@ export const BaseNavigationContainer = /*#__PURE__*/React.forwardRef(function Ba
       case 'REPLACE':
       case 'POP_TO':
       case 'JUMP_TO':
-        if (payload?.name) {
-          message += `\n\nDo you have a screen named '${payload.name}'?\n\nIf you're trying to navigate to a screen in a nested navigator, see https://reactnavigation.org/docs/nesting-navigators#navigating-to-a-screen-in-a-nested-navigator.\n\nIf you're using conditional rendering, navigation will happen automatically and you shouldn't navigate manually, see.`;
+        if (payload && payload.name) {
+          message += "\n\nDo you have a screen named '" + payload.name + "'?\n\nIf you're trying to navigate to a screen in a nested navigator, see https://reactnavigation.org/docs/nesting-navigators#navigating-to-a-screen-in-a-nested-navigator.\n\nIf you're using conditional rendering, navigation will happen automatically and you shouldn't navigate manually, see.";
         } else {
-          message += `\n\nYou need to pass the name of the screen to navigate to.\n\nSee https://reactnavigation.org/docs/navigation-actions for usage.`;
+          message += "\n\nYou need to pass the name of the screen to navigate to.\n\nSee https://reactnavigation.org/docs/navigation-actions for usage.";
         }
         break;
       case 'GO_BACK':
       case 'POP':
       case 'POP_TO_TOP':
-        message += `\n\nIs there any screen to go back to?`;
+        message += "\n\nIs there any screen to go back to?";
         break;
       case 'OPEN_DRAWER':
       case 'CLOSE_DRAWER':
       case 'TOGGLE_DRAWER':
-        message += `\n\nIs your screen inside a Drawer navigator?`;
+        message += "\n\nIs your screen inside a Drawer navigator?";
         break;
     }
-    message += `\n\nThis is a development-only warning and won't be shown in production.`;
+    message += "\n\nThis is a development-only warning and won't be shown in production.";
     console.error(message);
   });
   return /*#__PURE__*/_jsx(NavigationIndependentTreeContext.Provider, {
@@ -343,7 +349,7 @@ export const BaseNavigationContainer = /*#__PURE__*/React.forwardRef(function Ba
         children: /*#__PURE__*/_jsx(NavigationStateContext.Provider, {
           value: context,
           children: /*#__PURE__*/_jsx(UnhandledActionContext.Provider, {
-            value: onUnhandledAction ?? defaultOnUnhandledAction,
+            value: onUnhandledAction != null ? onUnhandledAction : defaultOnUnhandledAction,
             children: /*#__PURE__*/_jsx(DeprecatedNavigationInChildContext.Provider, {
               value: navigationInChildEnabled,
               children: /*#__PURE__*/_jsx(EnsureSingleNavigator, {
diff --git a/node_modules/@react-navigation/core/lib/module/PreventRemoveProvider.js b/node_modules/@react-navigation/core/lib/module/PreventRemoveProvider.js
index 1d4942d..8fe9c0d 100644
--- a/node_modules/@react-navigation/core/lib/module/PreventRemoveProvider.js
+++ b/node_modules/@react-navigation/core/lib/module/PreventRemoveProvider.js
@@ -17,7 +17,7 @@ const transformPreventedRoutes = preventedRoutesMap => {
     preventRemove
   }) => {
     acc[routeKey] = {
-      preventRemove: acc[routeKey]?.preventRemove || preventRemove
+      preventRemove: (acc[routeKey] ? acc[routeKey].preventRemove : undefined) || preventRemove
     };
     return acc;
   }, {});
@@ -36,14 +36,15 @@ export function PreventRemoveProvider({
   const route = React.useContext(NavigationRouteContext);
   const preventRemoveContextValue = React.useContext(PreventRemoveContext);
   // take `setPreventRemove` from parent context - if exist it means we're in a nested context
-  const setParentPrevented = preventRemoveContextValue?.setPreventRemove;
+  const setParentPrevented = preventRemoveContextValue ? preventRemoveContextValue.setPreventRemove : undefined;
   const setPreventRemove = useLatestCallback((id, routeKey, preventRemove) => {
-    if (preventRemove && (navigation == null || navigation?.getState().routes.every(route => route.key !== routeKey))) {
+    if (preventRemove && (navigation == null || navigation.getState().routes.every(route => route.key !== routeKey))) {
       throw new Error(`Couldn't find a route with the key ${routeKey}. Is your component inside NavigationContent?`);
     }
     setPreventedRoutesMap(prevPrevented => {
       // values haven't changed - do nothing
-      if (routeKey === prevPrevented.get(id)?.routeKey && preventRemove === prevPrevented.get(id)?.preventRemove) {
+      const previous = prevPrevented.get(id);
+      if (routeKey === (previous ? previous.routeKey : undefined) && preventRemove === (previous ? previous.preventRemove : undefined)) {
         return prevPrevented;
       }
       const nextPrevented = new Map(prevPrevented);
@@ -61,17 +62,18 @@ export function PreventRemoveProvider({
   const isPrevented = [...preventedRoutesMap.values()].some(({
     preventRemove
   }) => preventRemove);
+  const routeKey = route ? route.key : undefined;
   React.useEffect(() => {
-    if (route?.key !== undefined && setParentPrevented !== undefined) {
+    if (routeKey !== undefined && setParentPrevented !== undefined) {
       // when route is defined (and setParentPrevented) it means we're in a nested stack
       // route.key then will be the route key of parent
-      setParentPrevented(parentId, route.key, isPrevented);
+      setParentPrevented(parentId, routeKey, isPrevented);
       return () => {
-        setParentPrevented(parentId, route.key, false);
+        setParentPrevented(parentId, routeKey, false);
       };
     }
     return;
-  }, [parentId, isPrevented, route?.key, setParentPrevented]);
+  }, [parentId, isPrevented, routeKey, setParentPrevented]);
   const value = React.useMemo(() => ({
     setPreventRemove,
     preventedRoutes: transformPreventedRoutes(preventedRoutesMap)
diff --git a/node_modules/@react-navigation/core/lib/module/SceneView.js b/node_modules/@react-navigation/core/lib/module/SceneView.js
index d5abea9..666e05d 100644
--- a/node_modules/@react-navigation/core/lib/module/SceneView.js
+++ b/node_modules/@react-navigation/core/lib/module/SceneView.js
@@ -81,7 +81,7 @@ export function SceneView({
 
     // Add our state to the innermost route of the parent state
     const addState = parent => {
-      const parentRoute = parent?.routes[0];
+      const parentRoute = parent ? parent.routes[0] : undefined;
       if (parentRoute) {
         return {
           routes: [{
diff --git a/node_modules/@react-navigation/core/lib/module/StaticNavigation.js b/node_modules/@react-navigation/core/lib/module/StaticNavigation.js
index e20d571..d9c6bfb 100644
--- a/node_modules/@react-navigation/core/lib/module/StaticNavigation.js
+++ b/node_modules/@react-navigation/core/lib/module/StaticNavigation.js
@@ -233,11 +233,11 @@ export function createPathConfigForStaticNavigation(tree, options, auto) {
     // So we add the screens and groups in the same order as they are defined
     for (const key in t.config) {
       if (key === 'screens' && t.config.screens) {
-        Object.assign(screens, createPathConfigForScreens(t.config.screens, o?.initialRouteName ?? t.config.initialRouteName));
+        Object.assign(screens, createPathConfigForScreens(t.config.screens, o && o.initialRouteName != null ? o.initialRouteName : t.config.initialRouteName));
       }
       if (key === 'groups' && t.config.groups) {
         Object.entries(t.config.groups).forEach(([, group]) => {
-          Object.assign(screens, createPathConfigForScreens(group.screens, o?.initialRouteName ?? t.config.initialRouteName));
+          Object.assign(screens, createPathConfigForScreens(group.screens, o && o.initialRouteName != null ? o.initialRouteName : t.config.initialRouteName));
         });
       }
     }
diff --git a/node_modules/@react-navigation/core/lib/module/checkDuplicateRouteNames.js b/node_modules/@react-navigation/core/lib/module/checkDuplicateRouteNames.js
index 30be94d..d0cc8f6 100644
--- a/node_modules/@react-navigation/core/lib/module/checkDuplicateRouteNames.js
+++ b/node_modules/@react-navigation/core/lib/module/checkDuplicateRouteNames.js
@@ -5,11 +5,14 @@ export function checkDuplicateRouteNames(state) {
   const getRouteNames = (location, state) => {
     state.routes.forEach(route => {
       const currentLocation = location ? `${location} > ${route.name}` : route.name;
-      route.state?.routeNames?.forEach(routeName => {
-        if (routeName === route.name) {
-          duplicates.push([currentLocation, `${currentLocation} > ${route.name}`]);
-        }
-      });
+      const nestedRouteNames = route.state && route.state.routeNames ? route.state.routeNames : undefined;
+      if (nestedRouteNames) {
+        nestedRouteNames.forEach(routeName => {
+          if (routeName === route.name) {
+            duplicates.push([currentLocation, `${currentLocation} > ${route.name}`]);
+          }
+        });
+      }
       if (route.state) {
         getRouteNames(currentLocation, route.state);
       }
diff --git a/node_modules/@react-navigation/core/lib/module/deepFreeze.js b/node_modules/@react-navigation/core/lib/module/deepFreeze.js
index f5afb5c..c6e9e9f 100644
--- a/node_modules/@react-navigation/core/lib/module/deepFreeze.js
+++ b/node_modules/@react-navigation/core/lib/module/deepFreeze.js
@@ -23,7 +23,8 @@ export const deepFreeze = object => {
   for (const key in object) {
     // Don't freeze objects in params since they are passed by the user
     if (key !== 'params') {
-      if (Object.getOwnPropertyDescriptor(object, key)?.configurable) {
+      const descriptor = Object.getOwnPropertyDescriptor(object, key);
+      if (descriptor && descriptor.configurable) {
         const value = object[key];
         deepFreeze(value);
       }
diff --git a/node_modules/@react-navigation/core/lib/module/findFocusedRoute.js b/node_modules/@react-navigation/core/lib/module/findFocusedRoute.js
index 0468c4e..3394905 100644
--- a/node_modules/@react-navigation/core/lib/module/findFocusedRoute.js
+++ b/node_modules/@react-navigation/core/lib/module/findFocusedRoute.js
@@ -2,10 +2,15 @@
 
 export function findFocusedRoute(state) {
   let current = state;
-  while (current?.routes[current.index ?? 0].state != null) {
-    current = current.routes[current.index ?? 0].state;
+  while (current) {
+    const currentIndex = current.index != null ? current.index : 0;
+    const currentRoute = current.routes[currentIndex];
+    if (currentRoute == null || currentRoute.state == null) {
+      break;
+    }
+    current = currentRoute.state;
   }
-  const route = current?.routes[current?.index ?? 0];
+  const route = current ? current.routes[current.index != null ? current.index : 0] : undefined;
   return route;
 }
 //# sourceMappingURL=findFocusedRoute.js.map
diff --git a/node_modules/@react-navigation/core/lib/module/getActionFromState.js b/node_modules/@react-navigation/core/lib/module/getActionFromState.js
index 3fde1ba..5e46649 100644
--- a/node_modules/@react-navigation/core/lib/module/getActionFromState.js
+++ b/node_modules/@react-navigation/core/lib/module/getActionFromState.js
@@ -7,15 +7,15 @@ export function getActionFromState(state, options) {
   if (routes.length === 0) {
     return undefined;
   }
-  if (!(routes.length === 1 && routes[0].key === undefined || routes.length === 2 && routes[0].key === undefined && routes[0].name === normalizedConfig?.initialRouteName && routes[1].key === undefined)) {
+  if (!(routes.length === 1 && routes[0].key === undefined || routes.length === 2 && routes[0].key === undefined && routes[0].name === normalizedConfig.initialRouteName && routes[1].key === undefined)) {
     return {
       type: 'RESET',
       payload: state
     };
   }
-  const route = state.routes[state.index ?? state.routes.length - 1];
-  let current = route?.state;
-  let config = normalizedConfig?.screens?.[route?.name];
+  const route = state.routes[state.index != null ? state.index : state.routes.length - 1];
+  let current = route ? route.state : undefined;
+  let config = normalizedConfig.screens && route ? normalizedConfig.screens[route.name] : undefined;
   let params = {
     ...route.params
   };
@@ -39,7 +39,7 @@ export function getActionFromState(state, options) {
   // There are 2 ways we can detect if a screen contains a navigator:
   // - The route contains nested state in `route.state`
   // - Nested screens are defined in the config
-  if (payload && config?.screens && Object.keys(config.screens).length) {
+  if (payload && config && config.screens && Object.keys(config.screens).length) {
     payload.pop = true;
   }
   while (current) {
@@ -59,7 +59,7 @@ export function getActionFromState(state, options) {
     if (routes.length === 1 && routes[0].key === undefined) {
       params.initial = true;
       params.screen = route.name;
-    } else if (routes.length === 2 && routes[0].key === undefined && routes[0].name === config?.initialRouteName && routes[1].key === undefined) {
+    } else if (routes.length === 2 && routes[0].key === undefined && routes[0].name === (config ? config.initialRouteName : undefined) && routes[1].key === undefined) {
       params.initial = false;
       params.screen = route.name;
     } else {
@@ -77,12 +77,12 @@ export function getActionFromState(state, options) {
       params.params = route.params;
     }
     current = route.state;
-    config = config?.screens?.[route.name];
-    if (config?.screens && Object.keys(config.screens).length) {
+    config = config && config.screens ? config.screens[route.name] : undefined;
+    if (config && config.screens && Object.keys(config.screens).length) {
       params.pop = true;
     }
   }
-  if (payload?.params.screen || payload?.params.state) {
+  if (payload && payload.params && (payload.params.screen || payload.params.state)) {
     payload.pop = true;
   }
   if (!payload) {
diff --git a/node_modules/@react-navigation/core/lib/module/getFocusedRouteNameFromRoute.js b/node_modules/@react-navigation/core/lib/module/getFocusedRouteNameFromRoute.js
index d6f69bf..93e9096 100644
--- a/node_modules/@react-navigation/core/lib/module/getFocusedRouteNameFromRoute.js
+++ b/node_modules/@react-navigation/core/lib/module/getFocusedRouteNameFromRoute.js
@@ -3,16 +3,17 @@
 import { CHILD_STATE } from "./useRouteCache.js";
 export function getFocusedRouteNameFromRoute(route) {
   // @ts-expect-error: this isn't in type definitions coz we want this private
-  const state = route[CHILD_STATE] ?? route.state;
+  const state = route[CHILD_STATE] != null ? route[CHILD_STATE] : route.state;
   const params = route.params;
+  const routeIndex = state ? state.index != null ? state.index : typeof state.type === 'string' && state.type !== 'stack' ? 0 : state.routes.length - 1 : undefined;
   const routeName = state ?
   // Get the currently active route name in the nested navigator
   state.routes[
   // If we have a partial state without index, for tab/drawer, first screen will be focused one, and last for stack
   // The type property will only exist for rehydrated state and not for state from deep link
-  state.index ?? (typeof state.type === 'string' && state.type !== 'stack' ? 0 : state.routes.length - 1)].name :
+  routeIndex].name :
   // If state doesn't exist, we need to default to `screen` param if available
-  typeof params?.screen === 'string' ? params.screen : undefined;
+  params && typeof params.screen === 'string' ? params.screen : undefined;
   return routeName;
 }
 //# sourceMappingURL=getFocusedRouteNameFromRoute.js.map
diff --git a/node_modules/@react-navigation/core/lib/module/getPathFromState.js b/node_modules/@react-navigation/core/lib/module/getPathFromState.js
index ef4e3af..e1bf481 100644
--- a/node_modules/@react-navigation/core/lib/module/getPathFromState.js
+++ b/node_modules/@react-navigation/core/lib/module/getPathFromState.js
@@ -12,8 +12,8 @@ const getActiveRoute = state => {
 };
 const cachedNormalizedConfigs = new WeakMap();
 const getNormalizedConfigs = options => {
-  if (!options?.screens) return {};
-  const cached = cachedNormalizedConfigs.get(options?.screens);
+  if (!(options && options.screens)) return {};
+  const cached = cachedNormalizedConfigs.get(options.screens);
   if (cached) return cached;
   const normalizedConfigs = createNormalizedConfigs(options.screens);
   cachedNormalizedConfigs.set(options.screens, normalizedConfigs);
@@ -79,7 +79,8 @@ export function getPathFromState(state, options) {
         const currentParams = Object.fromEntries(Object.entries(route.params).map(([key, value]) => {
           if (value === undefined) {
             if (options) {
-              const optional = options.parts?.find(part => part.param === key)?.optional;
+              const matchingPart = options.parts ? options.parts.find(part => part.param === key) : undefined;
+              const optional = matchingPart ? matchingPart.optional : undefined;
               if (optional) {
                 return null;
               }
@@ -87,10 +88,10 @@ export function getPathFromState(state, options) {
               return null;
             }
           }
-          const stringify = options?.stringify?.[key] ?? String;
+          const stringify = options && options.stringify && options.stringify[key] ? options.stringify[key] : String;
           return [key, stringify(value)];
         }).filter(entry => entry != null));
-        if (parts?.length) {
+        if (parts && parts.length) {
           Object.assign(allParams, currentParams);
         }
         if (focusedRoute === route) {
@@ -99,9 +100,9 @@ export function getPathFromState(state, options) {
           focusedParams = {
             ...currentParams
           };
-          parts
+          if (parts) {
           // eslint-disable-next-line no-loop-func
-          ?.forEach(({
+            parts.forEach(({
             param
           }) => {
             if (param) {
@@ -112,6 +113,7 @@ export function getPathFromState(state, options) {
               }
             }
           });
+          }
         }
       }
 
@@ -134,7 +136,7 @@ export function getPathFromState(state, options) {
       }
     }
     if (currentOptions[route.name] !== undefined) {
-      path += parts?.map(({
+      path += (parts ? parts : []).map(({
         segment,
         param,
         optional
@@ -186,7 +188,7 @@ export function getPathFromState(state, options) {
   }
 
   // Include the root path if specified
-  if (options?.path) {
+  if (options && options.path) {
     path = `${options.path}/${path}`;
   }
 
diff --git a/node_modules/@react-navigation/core/lib/module/getPatternParts.js b/node_modules/@react-navigation/core/lib/module/getPatternParts.js
index 59b8008..b71372b 100644
--- a/node_modules/@react-navigation/core/lib/module/getPatternParts.js
+++ b/node_modules/@react-navigation/core/lib/module/getPatternParts.js
@@ -23,7 +23,7 @@ export function getPatternParts(path) {
       if (current.segment === ':') {
         isParam = true;
       } else if (!isRegex) {
-        throw new Error(`Encountered ':' in the middle of a segment in path: ${path}`);
+        throw new Error("Encountered ':' in the middle of a segment in path: " + path);
       }
     } else if (char === '(') {
       if (isParam) {
@@ -34,7 +34,7 @@ export function getPatternParts(path) {
           isRegex = true;
         }
       } else {
-        throw new Error(`Encountered '(' without preceding ':' in path: ${path}`);
+        throw new Error("Encountered '(' without preceding ':' in path: " + path);
       }
     } else if (char === ')') {
       if (isParam && isRegex) {
@@ -47,14 +47,14 @@ export function getPatternParts(path) {
           isParam = false;
         }
       } else {
-        throw new Error(`Encountered ')' without preceding '(' in path: ${path}`);
+        throw new Error("Encountered ')' without preceding '(' in path: " + path);
       }
     } else if (char === '?') {
       if (current.param) {
         isParam = false;
         current.optional = true;
       } else {
-        throw new Error(`Encountered '?' without preceding ':' in path: ${path}`);
+        throw new Error("Encountered '?' without preceding ':' in path: " + path);
       }
     } else if (char == null || char === '/' && !isRegex) {
       isParam = false;
@@ -88,12 +88,12 @@ export function getPatternParts(path) {
     }
   }
   if (isRegex) {
-    throw new Error(`Could not find closing ')' in path: ${path}`);
+    throw new Error("Could not find closing ')' in path: " + path);
   }
   const params = parts.map(part => part.param).filter(Boolean);
   for (const [index, param] of params.entries()) {
     if (params.indexOf(param) !== index) {
-      throw new Error(`Duplicate param name '${param}' found in path: ${path}`);
+      throw new Error("Duplicate param name '" + param + "' found in path: " + path);
     }
   }
   return parts;
diff --git a/node_modules/@react-navigation/core/lib/module/getStateFromPath.js b/node_modules/@react-navigation/core/lib/module/getStateFromPath.js
index 7132844..1605bb9 100644
--- a/node_modules/@react-navigation/core/lib/module/getStateFromPath.js
+++ b/node_modules/@react-navigation/core/lib/module/getStateFromPath.js
@@ -33,14 +33,14 @@ export function getStateFromPath(path, options) {
     initialRoutes,
     configs
   } = getConfigResources(options);
-  const screens = options?.screens;
+  const screens = options ? options.screens : undefined;
   let remaining = path.replace(/\/+/g, '/') // Replace multiple slash (//) with single ones
   .replace(/^\//, '') // Remove extra leading slash
   .replace(/\?.*$/, ''); // Remove query params which we will handle later
 
   // Make sure there is a trailing slash
   remaining = remaining.endsWith('/') ? remaining : `${remaining}/`;
-  const prefix = options?.path?.replace(/^\//, ''); // Remove extra leading slash
+  const prefix = options && options.path ? options.path.replace(/^\//, '') : undefined; // Remove extra leading slash
 
   if (prefix) {
     // Make sure there is a trailing slash
@@ -116,7 +116,7 @@ function prepareConfigResources(options) {
     validatePathConfig(options);
   }
   const initialRoutes = getInitialRoutes(options);
-  const configs = getSortedNormalizedConfigs(initialRoutes, options?.screens);
+  const configs = getSortedNormalizedConfigs(initialRoutes, options ? options.screens : undefined);
   checkForDuplicatedConfigs(configs);
   const configWithRegexes = getConfigsWithRegexes(configs);
   return {
@@ -127,7 +127,7 @@ function prepareConfigResources(options) {
 }
 function getInitialRoutes(options) {
   const initialRoutes = [];
-  if (options?.initialRouteName) {
+  if (options && options.initialRouteName) {
     initialRoutes.push({
       initialRouteName: options.initialRouteName,
       parentScreens: []
@@ -262,7 +262,7 @@ const matchAgainstConfigs = (remaining, configs) => {
         const params = routeConfig && match.groups ? Object.fromEntries(Object.entries(match.groups).map(([key, value]) => {
           const index = Number(key.replace('param_', ''));
           const param = routeConfig.params.find(it => it.index === index);
-          if (param?.screen === routeName && param?.name) {
+          if (param && param.screen === routeName && param.name) {
             return [param.name, value];
           }
           return null;
@@ -271,7 +271,7 @@ const matchAgainstConfigs = (remaining, configs) => {
             return [key, undefined];
           }
           const decoded = decodeURIComponent(value);
-          const parsed = routeConfig.parse?.[key] ? routeConfig.parse[key](decoded) : decoded;
+          const parsed = routeConfig.parse && routeConfig.parse[key] ? routeConfig.parse[key](decoded) : decoded;
           return [key, parsed];
         })) : undefined;
         if (params && Object.keys(params).length) {
@@ -346,7 +346,7 @@ const createNormalizedConfigs = (screen, routeConfig, initials, paths, parentScr
       configs.push(createConfigItem(screen, [...routeNames], [...paths], config.parse));
       configs.push(...aliasConfigs);
     }
-    if (typeof config !== 'string' && typeof config.path !== 'string' && config.alias?.length) {
+    if (typeof config !== 'string' && typeof config.path !== 'string' && config.alias && config.alias.length) {
       throw new Error(`Screen '${screen}' doesn't specify a 'path'. A 'path' needs to be specified in order to use 'alias'.`);
     }
     if (config.screens) {
diff --git a/node_modules/@react-navigation/core/lib/module/useDescriptors.js b/node_modules/@react-navigation/core/lib/module/useDescriptors.js
index 7f57f2c..80c8d93 100644
--- a/node_modules/@react-navigation/core/lib/module/useDescriptors.js
+++ b/node_modules/@react-navigation/core/lib/module/useDescriptors.js
@@ -100,11 +100,11 @@ export function useDescriptors({
     });
     const layout =
     // The `layout` prop passed to `Screen` elements,
-    screen.layout ??
+    (screen.layout != null ? screen.layout :
     // The `screenLayout` props passed to `Group` elements
-    config.layout ??
+    config.layout != null ? config.layout :
     // The default `screenLayout` passed to the navigator
-    screenLayout;
+    screenLayout);
     let element = /*#__PURE__*/_jsx(SceneView, {
       navigation: navigation,
       route: route,
diff --git a/node_modules/@react-navigation/core/lib/module/useEventEmitter.js b/node_modules/@react-navigation/core/lib/module/useEventEmitter.js
index 3a68aad..cb8e56d 100644
--- a/node_modules/@react-navigation/core/lib/module/useEventEmitter.js
+++ b/node_modules/@react-navigation/core/lib/module/useEventEmitter.js
@@ -48,7 +48,7 @@ export function useEventEmitter(listen) {
     const items = listeners.current[type] || {};
 
     // Copy the current list of callbacks in case they are mutated during execution
-    const callbacks = target !== undefined ? items[target]?.slice() : [].concat(...Object.keys(items).map(t => items[t])).filter((cb, i, self) => self.lastIndexOf(cb) === i);
+    const callbacks = target !== undefined ? (items[target] ? items[target].slice() : undefined) : [].concat(...Object.keys(items).map(t => items[t])).filter((cb, i, self) => self.lastIndexOf(cb) === i);
     const event = {
       get type() {
         return type;
@@ -87,8 +87,12 @@ export function useEventEmitter(listen) {
         }
       });
     }
-    listenRef.current?.(event);
-    callbacks?.forEach(cb => cb(event));
+    if (listenRef.current) {
+      listenRef.current(event);
+    }
+    if (callbacks) {
+      callbacks.forEach(cb => cb(event));
+    }
     return event;
   }, []);
   return React.useMemo(() => ({
diff --git a/node_modules/@react-navigation/core/lib/module/useFocusEvents.js b/node_modules/@react-navigation/core/lib/module/useFocusEvents.js
index 70670e6..c8a0475 100644
--- a/node_modules/@react-navigation/core/lib/module/useFocusEvents.js
+++ b/node_modules/@react-navigation/core/lib/module/useFocusEvents.js
@@ -15,20 +15,20 @@ export function useFocusEvents({
 
   // When the parent screen changes its focus state, we also need to change child's focus
   // Coz the child screen can't be focused if the parent screen is out of focus
-  React.useEffect(() => navigation?.addListener('focus', () => {
+  React.useEffect(() => navigation ? navigation.addListener('focus', () => {
     lastFocusedKeyRef.current = currentFocusedKey;
     emitter.emit({
       type: 'focus',
       target: currentFocusedKey
     });
-  }), [currentFocusedKey, emitter, navigation]);
-  React.useEffect(() => navigation?.addListener('blur', () => {
+  }) : undefined, [currentFocusedKey, emitter, navigation]);
+  React.useEffect(() => navigation ? navigation.addListener('blur', () => {
     lastFocusedKeyRef.current = undefined;
     emitter.emit({
       type: 'blur',
       target: currentFocusedKey
     });
-  }), [currentFocusedKey, emitter, navigation]);
+  }) : undefined, [currentFocusedKey, emitter, navigation]);
   React.useEffect(() => {
     const lastFocusedKey = lastFocusedKeyRef.current;
     lastFocusedKeyRef.current = currentFocusedKey;
diff --git a/node_modules/@react-navigation/core/lib/module/useFocusedListenersChildrenAdapter.js b/node_modules/@react-navigation/core/lib/module/useFocusedListenersChildrenAdapter.js
index a38889f..c31357b 100644
--- a/node_modules/@react-navigation/core/lib/module/useFocusedListenersChildrenAdapter.js
+++ b/node_modules/@react-navigation/core/lib/module/useFocusedListenersChildrenAdapter.js
@@ -37,6 +37,6 @@ export function useFocusedListenersChildrenAdapter({
       };
     }
   }, [focusedListeners, navigation]);
-  React.useEffect(() => addListener?.('focus', listener), [addListener, listener]);
+  React.useEffect(() => addListener ? addListener('focus', listener) : undefined, [addListener, listener]);
 }
 //# sourceMappingURL=useFocusedListenersChildrenAdapter.js.map
diff --git a/node_modules/@react-navigation/core/lib/module/useIsFocused.js b/node_modules/@react-navigation/core/lib/module/useIsFocused.js
index 029edfe..cb34a9f 100644
--- a/node_modules/@react-navigation/core/lib/module/useIsFocused.js
+++ b/node_modules/@react-navigation/core/lib/module/useIsFocused.js
@@ -30,6 +30,6 @@ export function useIsFocused() {
   // isFocused from context only works with NavigationProvider
   // So this is kept for backward compatibility
   const value = React.useSyncExternalStore(subscribe, navigation.isFocused, navigation.isFocused);
-  return isFocused ?? value;
+  return isFocused != null ? isFocused : value;
 }
 //# sourceMappingURL=useIsFocused.js.map
diff --git a/node_modules/@react-navigation/core/lib/module/useNavigation.js b/node_modules/@react-navigation/core/lib/module/useNavigation.js
index ee7916d..a0cfa04 100644
--- a/node_modules/@react-navigation/core/lib/module/useNavigation.js
+++ b/node_modules/@react-navigation/core/lib/module/useNavigation.js
@@ -16,6 +16,6 @@ export function useNavigation() {
   }
 
   // FIXME: Figure out a better way to do this
-  return navigation ?? root;
+  return navigation != null ? navigation : root;
 }
 //# sourceMappingURL=useNavigation.js.map
diff --git a/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js b/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js
index cd5e325..7ca270d 100644
--- a/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js
+++ b/node_modules/@react-navigation/core/lib/module/useNavigationBuilder.js
@@ -92,7 +92,8 @@ const getRouteConfigsFromChildren = (children, groupKey, groupOptions, groupLayo
         return acc;
       }
     }
-    throw new Error(`A navigator can only contain 'Screen', 'Group' or 'React.Fragment' as its direct children (found ${/*#__PURE__*/React.isValidElement(child) ? `'${typeof child.type === 'string' ? child.type : child.type?.name}'${child.props != null && typeof child.props === 'object' && 'name' in child.props && child.props?.name ? ` for the screen '${child.props.name}'` : ''}` : typeof child === 'object' ? JSON.stringify(child) : `'${String(child)}'`}). To render this component in the navigator, pass it in the 'component' prop to 'Screen'.`);
+    var foundPart = React.isValidElement(child) ? ("'" + (typeof child.type === 'string' ? child.type : (child.type && child.type.name)) + "'" + (child.props != null && typeof child.props === 'object' && 'name' in child.props && child.props.name ? (" for the screen '" + child.props.name + "'") : '')) : (typeof child === 'object' ? JSON.stringify(child) : ("'" + String(child) + "'"));
+    throw new Error("A navigator can only contain 'Screen', 'Group' or 'React.Fragment' as its direct children (found " + foundPart + "). To render this component in the navigator, pass it in the 'component' prop to 'Screen'.");
   }, []);
   if (process.env.NODE_ENV !== 'production') {
     configs.forEach(config => {
@@ -139,9 +140,9 @@ const getRouteConfigsFromChildren = (children, groupKey, groupOptions, groupLayo
   return configs;
 };
 const getStateFromParams = params => {
-  if (params?.state != null) {
+  if (params && params.state != null) {
     return params.state;
-  } else if (typeof params?.screen === 'string' && params?.initial !== false) {
+  } else if (params && typeof params.screen === 'string' && params.initial !== false) {
     return {
       routes: [{
         name: params.screen,
@@ -163,7 +164,8 @@ const getStateFromParams = params => {
 export function useNavigationBuilder(createRouter, options) {
   const navigatorKey = useRegisterNavigator();
   const route = React.useContext(NavigationRouteContext);
-  const isNestedParamsConsumed = typeof route?.params === 'object' && route.params != null ? CONSUMED_PARAMS in route.params && route.params[CONSUMED_PARAMS] === route.params : false;
+  const routeParams = route && typeof route.params === 'object' && route.params != null ? route.params : undefined;
+  const isNestedParamsConsumed = routeParams ? CONSUMED_PARAMS in routeParams && routeParams[CONSUMED_PARAMS] === routeParams : false;
   const {
     children,
     layout,
@@ -197,7 +199,7 @@ export function useNavigationBuilder(createRouter, options) {
   }, {});
   const routeNames = routeConfigs.map(config => config.props.name);
   const routeKeyList = routeNames.reduce((acc, curr) => {
-    acc[curr] = screens[curr].keys.map(key => key ?? '').join(':');
+    acc[curr] = screens[curr].keys.map(key => key != null ? key : '').join(':');
     return acc;
   }, {});
   const routeParamList = routeNames.reduce((acc, curr) => {
@@ -254,7 +256,7 @@ export function useNavigationBuilder(createRouter, options) {
       const {
         initialParams
       } = screens[curr].props;
-      const initialParamsFromParams = route?.params?.state == null && route?.params?.initial !== false && route?.params?.screen === curr ? route.params.params : undefined;
+      const initialParamsFromParams = route && route.params && route.params.state == null && route.params.initial !== false && route.params.screen === curr ? route.params.params : undefined;
       acc[curr] = initialParams !== undefined || initialParamsFromParams !== undefined ? {
         ...initialParams,
         ...initialParamsFromParams
@@ -266,16 +268,16 @@ export function useNavigationBuilder(createRouter, options) {
     // We also need to re-initialize it if the state passed from parent was changed (maybe due to reset)
     // Otherwise assume that the state was provided as initial state
     // So we need to rehydrate it to make it usable
-    if ((currentState === undefined || !isStateValid(currentState)) && route?.params?.state == null && !(typeof route?.params?.screen === 'string' && route?.params?.initial !== false) && !isNestedParamsConsumed) {
+    if ((currentState === undefined || !isStateValid(currentState)) && (!route || !route.params || route.params.state == null) && !(route && route.params && typeof route.params.screen === 'string' && route.params.initial !== false) && !isNestedParamsConsumed) {
       return [undefined, router.getInitialState({
         routeNames,
         routeParamList: initialRouteParamList,
         routeGetIdList
       }), true, undefined];
     } else {
-      const paramsForState = isNestedParamsConsumed ? undefined : route?.params;
+      const paramsForState = isNestedParamsConsumed ? undefined : route ? route.params : undefined;
       const stateFromParams = paramsForState ? getStateFromParams(paramsForState) : undefined;
-      const stateBeforeInitialization = stateFromParams ?? currentState;
+      const stateBeforeInitialization = stateFromParams != null ? stateFromParams : currentState;
       const hydratedState = stateBeforeInitialization == null ? router.getInitialState({
         routeNames,
         routeParamList: initialRouteParamList,
@@ -322,7 +324,7 @@ export function useNavigationBuilder(createRouter, options) {
   // Previously unhandled state is now valid again
   // And current state no longer has any valid routes
   // We should reuse the unhandled state instead of re-calculating the state
-  if (unhandledState?.routes.every(r => routeNames.includes(r.name)) && state?.routes.every(r => !routeNames.includes(r.name))) {
+  if (unhandledState && unhandledState.routes.every(r => routeNames.includes(r.name)) && state && state.routes.every(r => !routeNames.includes(r.name))) {
     shouldClearUnhandledState = true;
     nextState = router.getRehydratedState(unhandledState, {
       routeNames,
@@ -338,8 +340,8 @@ export function useNavigationBuilder(createRouter, options) {
       routeKeyChanges: Object.keys(routeKeyList).filter(name => name in previousRouteKeyList && routeKeyList[name] !== previousRouteKeyList[name])
     });
   }
-  let didConsumeNestedParams = route?.params === paramsUsedForInitialization;
-  if (route?.params && !didConsumeNestedParams) {
+  let didConsumeNestedParams = (route ? route.params : undefined) === paramsUsedForInitialization;
+  if (route && route.params && !didConsumeNestedParams) {
     let action;
     if (typeof route.params.state === 'object' && route.params.state != null && !isNestedParamsConsumed) {
       didConsumeNestedParams = true;
@@ -383,7 +385,7 @@ export function useNavigationBuilder(createRouter, options) {
     }) : nextState;
   }
   React.useEffect(() => {
-    if (didConsumeNestedParams && typeof route?.params === 'object' && route.params != null) {
+    if (didConsumeNestedParams && route && typeof route.params === 'object' && route.params != null) {
       // Track whether the params have been already consumed
       // Set it to the same object, so merged params can be handled again
       Object.defineProperty(route.params, CONSUMED_PARAMS, {
@@ -391,7 +393,7 @@ export function useNavigationBuilder(createRouter, options) {
         enumerable: false
       });
     }
-  }, [didConsumeNestedParams, route?.params]);
+  }, [didConsumeNestedParams, route ? route.params : undefined]);
   const shouldUpdate = state !== nextState;
   useScheduleUpdate(() => {
     if (shouldUpdate) {
@@ -456,12 +458,12 @@ export function useNavigationBuilder(createRouter, options) {
     let route;
     if (e.target) {
       route = state.routes.find(route => route.key === e.target);
-      if (route?.name) {
+      if (route && route.name) {
         routeNames.push(route.name);
       }
     } else {
       route = state.routes[state.index];
-      routeNames.push(...Object.keys(screens).filter(name => route?.name === name));
+      routeNames.push(...Object.keys(screens).filter(name => route && route.name === name));
     }
     if (route == null) {
       return;
@@ -479,12 +481,16 @@ export function useNavigationBuilder(createRouter, options) {
         route: route,
         navigation
       }) : listeners;
-      return map ? Object.keys(map).filter(type => type === e.type).map(type => map?.[type]) : undefined;
+      return map ? Object.keys(map).filter(type => type === e.type).map(type => map[type]) : undefined;
     }))
     // We don't want same listener to be called multiple times for same event
     // So we remove any duplicate functions from the array
     .filter((cb, i, self) => cb && self.lastIndexOf(cb) === i);
-    listeners.forEach(listener => listener?.(e));
+    listeners.forEach(listener => {
+      if (listener) {
+        listener(e);
+      }
+    });
   });
   useFocusEvents({
     state,
@@ -510,7 +516,7 @@ export function useNavigationBuilder(createRouter, options) {
     router,
     getState,
     setState,
-    key: route?.key,
+    key: route ? route.key : undefined,
     actionListeners: childListeners.action,
     beforeRemoveListeners: keyedListeners.beforeRemove,
     routerConfigOptions: {
@@ -522,7 +528,7 @@ export function useNavigationBuilder(createRouter, options) {
   });
   const onRouteFocus = useOnRouteFocus({
     router,
-    key: route?.key,
+    key: route ? route.key : undefined,
     getState,
     setState
   });
@@ -538,7 +544,9 @@ export function useNavigationBuilder(createRouter, options) {
       };
       setUnhandledState(state);
     }
-    onUnhandledActionParent?.(action);
+    if (onUnhandledActionParent) {
+      onUnhandledActionParent(action);
+    }
   });
   const navigation = useNavigationHelpers({
     id: options.id,
diff --git a/node_modules/@react-navigation/core/lib/module/useNavigationHelpers.js b/node_modules/@react-navigation/core/lib/module/useNavigationHelpers.js
index f697f9d..8172003 100644
--- a/node_modules/@react-navigation/core/lib/module/useNavigationHelpers.js
+++ b/node_modules/@react-navigation/core/lib/module/useNavigationHelpers.js
@@ -26,7 +26,9 @@ export function useNavigationHelpers({
       const action = typeof op === 'function' ? op(getState()) : op;
       const handled = onAction(action);
       if (!handled) {
-        onUnhandledAction?.(action);
+        if (onUnhandledAction) {
+          onUnhandledAction(action);
+        }
       }
     };
     const actions = {
@@ -46,11 +48,12 @@ export function useNavigationHelpers({
       isFocused: parentNavigationHelpers ? parentNavigationHelpers.isFocused : () => true,
       canGoBack: () => {
         const state = getState();
+        const parentCanGoBack = parentNavigationHelpers ? parentNavigationHelpers.canGoBack() : false;
         return router.getStateForAction(state, CommonActions.goBack(), {
           routeNames: state.routeNames,
           routeParamList: {},
           routeGetIdList: {}
-        }) !== null || parentNavigationHelpers?.canGoBack() || false;
+        }) !== null || parentCanGoBack || false;
       },
       getId: () => navigatorId,
       getParent: id => {
diff --git a/node_modules/@react-navigation/core/lib/module/useOnAction.js b/node_modules/@react-navigation/core/lib/module/useOnAction.js
index 7589ba4..3f3f175 100644
--- a/node_modules/@react-navigation/core/lib/module/useOnAction.js
+++ b/node_modules/@react-navigation/core/lib/module/useOnAction.js
@@ -94,7 +94,7 @@ export function useOnAction({
     emitter,
     beforeRemoveListeners
   });
-  React.useEffect(() => addListenerParent?.('action', onAction), [addListenerParent, onAction]);
+  React.useEffect(() => addListenerParent ? addListenerParent('action', onAction) : undefined, [addListenerParent, onAction]);
   return onAction;
 }
 //# sourceMappingURL=useOnAction.js.map
diff --git a/node_modules/@react-navigation/core/lib/module/useOnGetState.js b/node_modules/@react-navigation/core/lib/module/useOnGetState.js
index 4218f27..27e1c80 100644
--- a/node_modules/@react-navigation/core/lib/module/useOnGetState.js
+++ b/node_modules/@react-navigation/core/lib/module/useOnGetState.js
@@ -18,7 +18,8 @@ export function useOnGetState({
 
     // Avoid returning new route objects if we don't need to
     const routes = state.routes.map(route => {
-      const childState = getStateListeners[route.key]?.();
+      const listener = getStateListeners[route.key];
+      const childState = listener ? listener() : undefined;
       if (route.state === childState) {
         return route;
       }
@@ -36,7 +37,7 @@ export function useOnGetState({
     };
   }, [getState, getStateListeners]);
   React.useEffect(() => {
-    return addKeyedListener?.('getState', key, getRehydratedState);
+    return addKeyedListener ? addKeyedListener('getState', key, getRehydratedState) : undefined;
   }, [addKeyedListener, getRehydratedState, key]);
 }
 //# sourceMappingURL=useOnGetState.js.map
diff --git a/node_modules/@react-navigation/core/lib/module/useOnPreventRemove.js b/node_modules/@react-navigation/core/lib/module/useOnPreventRemove.js
index 5f6044e..43d6a37 100644
--- a/node_modules/@react-navigation/core/lib/module/useOnPreventRemove.js
+++ b/node_modules/@react-navigation/core/lib/module/useOnPreventRemove.js
@@ -11,7 +11,7 @@ export const shouldPreventRemove = (emitter, beforeRemoveListeners, currentRoute
   const removedRoutes = currentRoutes.filter(route => !nextRouteKeys.includes(route.key)).reverse();
   const visitedRouteKeys =
   // @ts-expect-error: add this property to mark that we've already emitted this action
-  action[VISITED_ROUTE_KEYS] ?? new Set();
+  action[VISITED_ROUTE_KEYS] != null ? action[VISITED_ROUTE_KEYS] : new Set();
   const beforeRemoveAction = {
     ...action,
     [VISITED_ROUTE_KEYS]: visitedRouteKeys
@@ -23,7 +23,8 @@ export const shouldPreventRemove = (emitter, beforeRemoveListeners, currentRoute
     }
 
     // First, we need to check if any child screens want to prevent it
-    const isPrevented = beforeRemoveListeners[route.key]?.(beforeRemoveAction);
+    const listener = beforeRemoveListeners[route.key];
+    const isPrevented = listener ? listener(beforeRemoveAction) : undefined;
     if (isPrevented) {
       return true;
     }
@@ -51,13 +52,13 @@ export function useOnPreventRemove({
     addKeyedListener
   } = React.useContext(NavigationBuilderContext);
   const route = React.useContext(NavigationRouteContext);
-  const routeKey = route?.key;
+  const routeKey = route ? route.key : undefined;
   React.useEffect(() => {
     if (routeKey) {
-      return addKeyedListener?.('beforeRemove', routeKey, action => {
+      return addKeyedListener ? addKeyedListener('beforeRemove', routeKey, action => {
         const state = getState();
         return shouldPreventRemove(emitter, beforeRemoveListeners, state.routes, [], action);
-      });
+      }) : undefined;
     }
   }, [addKeyedListener, beforeRemoveListeners, emitter, getState, routeKey]);
 }
diff --git a/node_modules/@react-navigation/core/lib/module/useOptionsGetters.js b/node_modules/@react-navigation/core/lib/module/useOptionsGetters.js
index 091f4de..bc86ebf 100644
--- a/node_modules/@react-navigation/core/lib/module/useOptionsGetters.js
+++ b/node_modules/@react-navigation/core/lib/module/useOptionsGetters.js
@@ -17,21 +17,22 @@ export function useOptionsGetters({
     addOptionsGetter: parentAddOptionsGetter
   } = React.useContext(NavigationStateContext);
   const optionsChangeListener = React.useCallback(() => {
-    const isFocused = navigation?.isFocused() ?? true;
+    const isFocused = navigation && navigation.isFocused ? navigation.isFocused() : true;
     const hasChildren = Object.keys(optionsGettersFromChildRef.current).length;
     if (isFocused && !hasChildren) {
-      onOptionsChange(optionsRef.current ?? {});
+      onOptionsChange(optionsRef.current != null ? optionsRef.current : {});
     }
   }, [navigation, onOptionsChange]);
   React.useEffect(() => {
     optionsRef.current = options;
     optionsChangeListener();
-    return navigation?.addListener('focus', optionsChangeListener);
+    return navigation && navigation.addListener ? navigation.addListener('focus', optionsChangeListener) : undefined;
   }, [navigation, options, optionsChangeListener]);
   const getOptionsFromListener = React.useCallback(() => {
     for (const key in optionsGettersFromChildRef.current) {
       if (key in optionsGettersFromChildRef.current) {
-        const result = optionsGettersFromChildRef.current[key]?.();
+        const getter = optionsGettersFromChildRef.current[key];
+        const result = getter ? getter() : undefined;
 
         // null means unfocused route
         if (result !== null) {
@@ -42,7 +43,7 @@ export function useOptionsGetters({
     return null;
   }, []);
   const getCurrentOptions = React.useCallback(() => {
-    const isFocused = navigation?.isFocused() ?? true;
+    const isFocused = navigation && navigation.isFocused ? navigation.isFocused() : true;
     if (!isFocused) {
       return null;
     }
@@ -53,7 +54,7 @@ export function useOptionsGetters({
     return optionsRef.current;
   }, [navigation, getOptionsFromListener]);
   React.useEffect(() => {
-    return parentAddOptionsGetter?.(key, getCurrentOptions);
+    return parentAddOptionsGetter ? parentAddOptionsGetter(key, getCurrentOptions) : undefined;
   }, [getCurrentOptions, parentAddOptionsGetter, key]);
   const addOptionsGetter = React.useCallback((key, getter) => {
     optionsGettersFromChildRef.current[key] = getter;
diff --git a/node_modules/@react-navigation/core/lib/module/usePreventRemove.js b/node_modules/@react-navigation/core/lib/module/usePreventRemove.js
index 51d9444..994203d 100644
--- a/node_modules/@react-navigation/core/lib/module/usePreventRemove.js
+++ b/node_modules/@react-navigation/core/lib/module/usePreventRemove.js
@@ -37,6 +37,6 @@ export function usePreventRemove(preventRemove, callback) {
       data: e.data
     });
   });
-  React.useEffect(() => navigation?.addListener('beforeRemove', beforeRemoveListener), [navigation, beforeRemoveListener]);
+  React.useEffect(() => navigation ? navigation.addListener('beforeRemove', beforeRemoveListener) : undefined, [navigation, beforeRemoveListener]);
 }
 //# sourceMappingURL=usePreventRemove.js.map
diff --git a/node_modules/@react-navigation/core/lib/module/validatePathConfig.js b/node_modules/@react-navigation/core/lib/module/validatePathConfig.js
index 1af1990..3d80e92 100644
--- a/node_modules/@react-navigation/core/lib/module/validatePathConfig.js
+++ b/node_modules/@react-navigation/core/lib/module/validatePathConfig.js
@@ -1,20 +1,20 @@
 "use strict";
 
-const formatToList = items => Object.entries(items).map(([key, value]) => `- ${key} (${value})`).join('\n');
+const formatToList = items => Object.entries(items).map(([key, value]) => '- ' + key + ' (' + value + ')').join('\n');
 export function validatePathConfig(config, root = true) {
-  const validation = {
+  const nestedValidation = root ? null : {
+    alias: 'array',
+    exact: 'boolean',
+    stringify: 'object',
+    parse: 'object'
+  };
+  const validation = Object.assign({
     path: 'string',
     initialRouteName: 'string',
-    screens: 'object',
-    ...(root ? null : {
-      alias: 'array',
-      exact: 'boolean',
-      stringify: 'object',
-      parse: 'object'
-    })
-  };
+    screens: 'object'
+  }, nestedValidation || {});
   if (typeof config !== 'object' || config === null) {
-    throw new Error(`Expected the configuration to be an object, but got ${JSON.stringify(config)}.`);
+    throw new Error('Expected the configuration to be an object, but got ' + JSON.stringify(config) + '.');
   }
   const validationErrors = Object.fromEntries(Object.keys(config).map(key => {
     if (key in validation) {
@@ -24,10 +24,10 @@ export function validatePathConfig(config, root = true) {
       if (value !== undefined) {
         if (type === 'array') {
           if (!Array.isArray(value)) {
-            return [key, `expected 'Array', got '${typeof value}'`];
+            return [key, "expected 'Array', got '" + typeof value + "'"];
           }
         } else if (typeof value !== type) {
-          return [key, `expected '${type}', got '${typeof value}'`];
+          return [key, "expected '" + type + "', got '" + typeof value + "'"];
         }
       }
     } else {
@@ -36,10 +36,10 @@ export function validatePathConfig(config, root = true) {
     return null;
   }).filter(Boolean));
   if (Object.keys(validationErrors).length) {
-    throw new Error(`Found invalid properties in the configuration:\n${formatToList(validationErrors)}\n\nYou can only specify the following properties:\n${formatToList(validation)}\n\nIf you want to specify configuration for screens, you need to specify them under a 'screens' property.\n\nSee https://reactnavigation.org/docs/configuring-links for more details on how to specify a linking configuration.`);
+    throw new Error('Found invalid properties in the configuration:\n' + formatToList(validationErrors) + '\n\nYou can only specify the following properties:\n' + formatToList(validation) + "\n\nIf you want to specify configuration for screens, you need to specify them under a 'screens' property.\n\nSee https://reactnavigation.org/docs/configuring-links for more details on how to specify a linking configuration.");
   }
   if (root && 'path' in config && typeof config.path === 'string' && config.path.includes(':')) {
-    throw new Error(`Found invalid path '${config.path}'. The 'path' in the top-level configuration cannot contain patterns for params.`);
+    throw new Error("Found invalid path '" + config.path + "'. The 'path' in the top-level configuration cannot contain patterns for params.");
   }
   if ('screens' in config && config.screens) {
     Object.entries(config.screens).forEach(([_, value]) => {
